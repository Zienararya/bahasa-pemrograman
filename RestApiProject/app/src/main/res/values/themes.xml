<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RestApiProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/Blue_Green</item>
        <item name="colorPrimary">@color/Blue_Green</item>
    </style>

    <style name="Theme.RestApiProject" parent="Base.Theme.RestApiProject" />
</resources>